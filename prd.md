# 面试 Copilot（GhostPilot）产品需求文档

| 文档版本 | 日期 | 作者 | 变更说明 |
|---------|------|------|---------|
| v1.0 | 2026-04-26 | GhostPilot 团队 | 初始版本 |
| v2.0 | 2026-04-26 | GhostPilot 团队 | 整合性能优化与隐身层设计 |
| v2.1 | 2026-04-26 | GhostPilot 团队 | 新增截图即时解答模块 (Alt+P) |

---

## 1. 产品概述

### 1.1 产品愿景
打造一款桌面端面试辅助工具，在候选人参加视频面试时，通过不可被屏幕共享捕获的透明悬浮窗，实时提供高度匹配面试问题的 AI 回答、解题思路与关键词提示，帮助候选人更自信、更流畅地展现专业能力。

### 1.2 核心价值
- **隐形辅助**：答案窗口仅候选人肉眼可见，屏幕录制、共享、截图均无法捕获。
- **极速响应**：从面试官提问结束到悬浮窗出现首个字 ≤ 0.5 秒，完整答案 ≤ 1.5 秒；截图识别首 token ≤ 2 秒。
- **专业应答**：针对行为面试、技术八股、算法白板题，分别采用 STAR 法则、技术要点、伪代码等结构化输出。支持截图类图形题的直接解答。

### 1.3 产品边界
- **仅限桌面平台**：优先支持 Windows 10 2004+，macOS 后续迭代。
- **不侵入面试软件**：作为独立进程运行，不修改任何面试客户端（Zoom、Teams、腾讯会议等）。
- **本地数据处理优先**：简历、JD 等资料在会话期间于本地向量化存储。仅在触发回答时，提取最相关的文本片段作为加密请求发送至云端 LLM（承诺使用不用于模型训练的商业 API），全量文件绝不上传。

---

## 2. 用户画像与典型场景

### 2.1 目标用户
- 参加线上技术面试的软件工程师、数据科学家、产品经理。
- 期望在算法、系统设计、行为面试中获得即时辅助的求职者。

### 2.2 典型场景
1. **算法白板题**：面试官在 Zoom 共享屏幕展示题目，候选人观看题目的同时，悬浮窗显示伪代码与解题思路。
2. **技术概念问答**：面试官口述问题（如“请解释 CAP 定理”），AI 在 1 秒内给出关键概念、表述模板。
3. **行为面试（BQ）**：面试官提问“讲一个你解决冲突的经历”，AI 基于简历生成 STAR 法则回答框架，候选人自然组织语言。
4. **图形/图表题**：面试官共享白板绘制架构图、UML 图或手写公式，候选人按下 `Alt+P` 截图，AI 直接识别图像内容并提供分析和答案。

---

## 3. 功能需求

### 3.1 音频采集模块（Audio Capture）

**目标**：获取面试官话语，语音转文本延迟 < 300ms。

**实现要点**：
- **系统音频回环**：基于 WASAPI Loopback （Windows）获取系统播放音频，避免依赖浏览器 WebRTC 采集。**需支持音频设备的手动选择与热插拔重连机制。**
- **静音补帧机制**：当系统无声音播放时，WASAPI Loopback 会停止输出数据。需实现自动补齐全 0 静音帧（Silence Padding）以维持下游 ASR 引擎的心跳。
- **音频参数**：16kHz 单声道 16-bit PCM，避免编码转换损耗。
- **缓冲区设计**：160ms 缓冲帧，积满即推向 ASR 流水线，不等待静音（VAD）判决完整句。
- **进程间通信**：音频采集独立进程，通过 Named Pipe（Windows）或 Unix Domain Socket（macOS）传递裸 PCM 至 ASR 进程。

### 3.2 实时语音识别模块（ASR）

**目标**：将音频流转换成文本，支持流式中间结果。

**需求**：
- 支持流式 ASR，每 200~300ms 产出 partial transcript。
- 置信度 ≥ 0.85 的中间结果立即下发给下游（问题分类、LLM 预热），不等 final。
- 提供商候选：
  - **Azure Speech SDK** (recognizing 事件)
  - **Google Cloud Speech** (interim_results)
  - **Deepgram Nova-2**（端到端延迟低，拟合面试场景 RTT 权重 > 准确率）
- 断句逻辑：由组合策略判定“问题成型”——
  - 句末标点（。？！）出现；
  - VAD 检测 >400ms 静默；
  - 轻量语义完整性二分类模型。
  满足任一条件立即将当前句子缓存作为完整问题进行下游推理。
- **候选人防干扰与并发策略**：
  - 需结合默认麦克风的 VAD 检测，当候选人正在发言时，抑制面试官背景音引起的无效触发。
  - 对于面试官的连续快速追问，悬浮窗需采取“队列追加”而非“强制覆盖”策略，避免新答案闪烁冲掉候选人正在阅读的旧答案。

### 3.3 AI 核心处理引擎（AI Engine）

**目标**：理解问题并生成精准回答。

**需求**：
- **大模型接入**：支持 GPT-4o / Claude 3.5 Sonnet / DeepSeek-R1 等，根据问题类型动态路由。
- **回答策略**：
  - **算法/白板题**：提供代码伪代码、关键数据结构、复杂度分析；
  - **行为面试**：基于简历与职位描述（JD）生成 STAR（情境-任务-行动-结果）框架；
  - **技术问答**：输出概念定义、核心要点、易错提醒。
- **上下文注入**（最小化原则）：
  - 与问题最相关的简历 3 个要点；
  - JD 前 500 字；
  - 公司背景 100 字。
  避免注入完整简历导致模型注意力分散、响应变慢。
- **流式生成与分段**：
  - 短答案（50-80 字）：一次生成；
  - 中答案（100-150 字）：边生成边推送到悬浮窗，前端消费 SSE；
  - 长答案（200-250 字）：分两段生成（骨架+细节），首段骨架保证 TTFT。
- **停词 guard**：检测重复、无限续写，SSE 层强制截止。

### 3.4 隐形提示视图（Invisible Overlay）

**目标**：显示答案但不能被屏幕共享、录制捕获。

**需求**：
- **极致透明**：背景完全透明，仅渲染文字，可调节透明度和字体大小。
- **OS 级隐身**：
  - **Windows**：调用 `SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE)`，窗口仅在本地显示，不出现于任何捕获流。
  - **macOS**：设置 `NSWindow.SharingType` 为 `.none`。
  - 行为：屏幕共享不可见、录屏不可见、Alt+Tab 缩略图不出现、任务栏不显示、Mission Control 不出现。
- **置顶与鼠标穿透**：窗口始终位于面试应用之上，**必须搭配 `WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_TOPMOST` 窗口扩展样式（Windows），确保鼠标点击完全穿透，避免阻挡候选人操作面试软件。**
- **关键词高亮**：算法题中的复杂度、方法名、关键字加粗/变色。

### 3.5 辅助工具箱（Toolbox）

- **知识库速查**：内置高频算法模板、系统设计常见架构、语言语法 Cheatsheet。**为避免打字声音引起警觉，需提供纯鼠标点击导航的树状目录或预设标签筛选机制，避免键盘输入搜索。**
- **自动复盘报告**：面试结束后自动整合问答记录（含截图题分析），生成面试总结（问题、AI 回答、改进建议），支持一键删除所有会话数据。
- **窗口微调面板**：可通过全局快捷键呼出设置界面对透明度、字体、窗口大小进行调节，设置界面本身也应具备防捕获能力。

### 3.6 截图即时解答模块（Visual Snapshot Solver）  [新增 v2.1]

**目标**：当面试官通过屏幕共享展示无法复制为文本的图形题目（白板手写、架构图、UML、算法图等）时，候选人按下快捷键即时获得 AI 分析。

**触发方式**：全局快捷键 `Alt + P`（可自定义）。

**处理流程**（独立于音频 Pipeline）：
1. **截图捕获**：
   - 调用系统 API 截取当前整个屏幕或活动窗口（用户可通过设置选择“全屏”或“当前窗口”）。
   - 截图操作在后台完成，不显示任何可视化提示，不触发共享屏幕的录屏通知。
2. **预处理**：
   - 必须处理 Windows High DPI 缩放（Per-Monitor v2），确保在高缩放率下系统截图坐标和内容准确不截断。
   - 将截图转换为 JPEG 格式，按比例压缩图像尺寸（保留代码等密集文本的清晰度，不使用固定宽度），以平衡传输延迟与识别准确率。
   - 可选：对图像进行自动裁剪，去除明显黑边或多余桌面区域。
3. **模型请求**：
   - 携带**预设系统提示**（可配置）：“你是一位资深技术面试官辅导专家。根据提供的截图中的题目，给出简洁清晰的解答，包括思路、要点、必要的代码/伪代码。如果是设计题，请给出架构要点。回答控制在 200 字以内。”
   - 请求直接发送至支持视觉理解的大模型（如 `gemini-1.5-pro` 或 `gpt-4o`），使用其图像输入能力。
   - **不走**常规的 ASR → 问题分类 → LLM 路由的音频 Pipeline，完全独立调用。
4. **流式展示**：
   - 模型返回的 SSE 流式文本逐 token 推送到隐形悬浮窗。
   - 悬浮窗自动调整大小以适应内容，正常显示透明样式和关键词高亮。
5. **数据清理**：
   - 截图数据仅在内存中短暂保留，传输后立即销毁；不写入磁盘，不进入任何日志。
   - 会话结束时，所有提问过的截图问答记录随其他数据一并清除（若用户开启自动删除）。

**延迟目标**：从按下 `Alt+P` 到悬浮窗出现首个 token ≤ 2 秒。此目标假设网络与模型服务正常，截图预处理 ≤ 200ms，首 token 生成 ≤ 1.8 秒。

**配置项**：
- 截图区域选择（全屏 / 当前活动窗口）
- 自定义系统提示文本
- 所用视觉模型（需用户提供有效 API Key 或选择内置模型）
- 图像压缩质量与尺寸上限

**异常处理**：
- 模型调用超时（>5 秒）：显示“识别超时，请重试”。
- 无 API Key 或权限错误：提示配置错误。
- 短暂禁用悬浮窗显示，仅播放协振动效或托盘通知，避免频繁弹出窗口干扰。

---

## 4. 非功能性需求

| 指标 | 目标值 | 备注 |
|------|--------|------|
| 端到端延迟（首字出现）—音频路径 | ≤ 500ms | 从问题判定完成到浮窗首个 token 展示 |
| 端到端延迟（完整回答）—音频路径 | ≤ 1500ms | 100-150 字场景典型值 |
| 音频采集到 ASR 中间结果 | ≤ 300ms | partial 首次产出 |
| 截图请求到首 token（Alt+P） | ≤ 2000ms | 新增视觉路径，含截图、压缩、传输 |
| 资源占用（CPU） | ≤ 5%（四核 i7 基准） | 正常运行时（不含截图识别时的瞬间占用） |
| 内存占用 | ≤ 500MB | 包含本地小模型，截图瞬时增量 < 50MB |
| 防检测能力 | 屏幕共享、录屏、截图零捕获 | 通过 OS 级 API 保证；Alt+P 截图产物不可见 |
| 稳定性 | 连续运行 3 小时无崩溃 | 覆盖超长面试 |

---

## 5. 性能优化方案（总览）

为了在 2 秒预算内完成“听-想-答”全流程，系统采用流式并行流水线 + 投机预热 + 分层模型路由。截图路径独立优化。

### 5.1 端到端流水线（Pipeline）
音频采集、ASR、问题分类、LLM 推理四个阶段完全解耦，通过异步队列（`asyncio.Queue`）并行运行，替代传统串行 `await` 链。
```
音频缓冲 → [Queue] → ASR Worker ─→ [Queue] → 分类器 Worker ─→ [Queue] → LLM Worker
```
所有模块以协程 Task 运行，实现单进程内的高效并发。截图模块为独立热键监听线程，触发后直接调起视觉模型请求，不与上述队列交互。

### 5.2 ASR 流式提前下发
- 不等待 final transcript，利用 `recognizing` 事件输出的 partial 文本（置信度 >0.85，长度 >8 字）直接推入 LLM 准备队列。
- 使 ASR 阶段延迟从传统 600-900ms 压缩到 200-300ms。

### 5.3 问题预判与上下文预热
- 问题分类器使用小 embedding 模型 + KNN，根据 partial text 判定问题类型。
- **提示词缓存 (Prompt Caching)**：针对公有云 LLM（如 Claude 3.5 / GPT-4o），在会话初始化时提前发送 System Prompt 和简历背景，利用服务端 Caching 机制，使后续请求的 TTFT 大幅降低。
- **投机推理 (针对本地模型)**：若部署本地模型，可根据 partial text 提前推入前几个字进行 KV Cache 预填充（Prefill）。

### 5.4 LLM 流式生成与首 token 优化
- 采用小参数量模型（如 GPT-4o-mini、Qwen-Plus）做首句生成，TTFT 比全尺寸模型快一倍。
- 精简 System Prompt，移除冗余词，降低预填充时间。
- 启用 Prompt Caching：重复的 System 部分利用服务端 KV Cache，TTFT 直接减半。
- 就近部署端点（国内区域），跨境调用减少 200ms+ 网络 RTT。

### 5.5 RAG 检索去延迟
- **预建向量库**：会话开始时，对简历和 JD 一次性切片、计算 embedding、存入本地 SQLite（sqlite-vec 扩展），session 内不变。
- **LRU 缓存**：问题文本归一化后作为 key 缓存 embedding，命中率 ~35%。
- **本地 embedding 模型**：使用 bge-small-zh-v1.5 ONNX 版本（~80MB），单次推理 40ms；云端仅用于 rerank，减少网络开销。

### 5.6 分段生成决策
根据问题预估答案长度动态选择生成策略：
- ≤80 字：单段完整生成。
- 100-150 字：SSE 流式推送。
- ≥200 字：先生成骨架（如 STAR 四要素关键词），立即展示；随后补充细节，提升感知速度。

### 5.7 模型动态路由
决定不在主链路中实时判断模型，而是在会话启动时根据 JD 类型预设路由规则：
- 行为面试：DeepSeek-R1；
- 技术概念：DeepSeek-R1；
- 代码题：DeepSeek-R1；
- **截图解题（视觉）：直接 Alt+P 调用，路由至 Gemini-1.5 Pro 或 GPT-4o，不参与音频问答的路由决策。**

---

## 6. 安全与隐私设计

- **数据隐私与处理边界**：音频流、本地简历库、JD 仅存在于本地进程内存与本地 SQLite 向量库。ASR 和 LLM 若使用云端 API，则仅上传必要的问题文本片段和极其有限的上下文切片进行加密传输，严格禁止用于模型训练；截图数据仅内存传输，不落盘。
- **会话自毁**：用户可一键删除所有日志、缓存、embedding 及截图问答记录，不可恢复。
- **窗口隐身**：OS 级 API 保证禁止捕获，不依赖窗口属性或透明技巧。
- **授权与认证**：本地客户端用设备绑定 License Key，防止滥用。

---

## 7. UI/UX 要求

- **提示窗**：无边框、背景透明、置顶、默认位置可拖拽。
- **可调节**：透明度滑块、字体缩放、关键词高亮配色。
- **任务管理**：系统托盘小图标（可设置隐藏），右键菜单提供“暂停辅助”“打开设置”“退出”等。
- **交互反馈**：音频采集状态指示（小圆点）、AI 思考中动画（隐形闪烁光点），仅在窗口区域柔和提示。截图识别时在悬浮窗左下角显示微型进度条（透明背景，不被捕获），或通过托盘图标动态提示，避免视觉干扰。

---

## 8. 工程踩坑与风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 浏览器 WebRTC 无法抓取系统音频 | 无法使用纯 Web 方案 | 原生 WASAPI 回环采集，独立进程 |
| STT 高准确率模型延迟过大 | 超预算 | 选择 Deepgram Nova-2 等低延迟引擎，容忍 1-2% 准确率下降 |
| LLM 无限续写、啰嗦 | 答案过长、延迟激增 | 强制停止 token 检测，限制最大 token 数 |
| 上下文过多导致 LLM 分心 | 回答质量下降 | 仅注入最相关摘要，篇幅严格限制 |
| Embedding 缓存命中率低 | 影响延迟 | 文本归一化（去标点、统一大小写），提升命中 |
| SetWindowDisplayAffinity 不被旧系统支持 | Windows 10 2004 以下无法隐身 | 最低系统要求明确，并检测降级提醒 |
| Alt+P 截图可能被安全软件拦截 | 快捷键冲突或权限问题 | 提供自定义快捷键选项，使用常见组合避免；请求管理员权限时降级为前台提示 |
| 视觉模型返回无关或过长内容 | 干扰候选人心流，阅读耗时 | 预设系统提示严格限制字数与格式；前端截断显示 |
| 本地 ONNX C端部署复杂 | 缺少 C 端 Tokenizer 且包体积大增 | 考虑引入 `tokenizers-cpp` 或在早期降级为云端轻量方案或简易 Chroma 模式 |

---

## 9. 附录：关键 API 与兼容性

### 9.1 Windows Stealth
- `SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE)`，要求调用窗口属于当前进程且为顶层窗口。
- 支持版本：Windows 10 Build 19041（2004）及以上。

### 9.2 推荐 STT SDK
- Azure Cognitive Services Speech SDK（流式、partial 事件）
- Google Cloud Speech-to-Text v2（interim results）
- Deepgram SDK（低延迟）

### 9.3 LLM 提供商
- OpenAI GPT-4o / GPT-4o-mini（stream=True，SSE；支持图像输入）
- Anthropic Claude 3.5 Sonnet（streaming，暂不原生支持图像可通过 Base64 传入）
- DeepSeek-R1（文本，用于音频路径）
- Google Gemini-1.5 Pro（视觉模型，用于截图路径）

### 9.4 本地 Embedding
- bge-small-zh-v1.5 转 ONNX，搭配 sqlite-vec 实现本地检索。

---

*文档状态：v2.1，已包含截图即时解答模块，待研发团队最终评审。*